<h1>Index</h1>

<ul>
  <% @pets.each do |pet| %>
    <li><input class="form-check-input" type="checkbox" value="" id="flexCheckDefault" <%= "checked" if pet.found %>> <%= link_to pet.name, pet_path(pet) %></li>
  <% end %>
</ul>

<%= link_to "Create new report", new_pet_path %>
